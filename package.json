{
  "name": "whatcoinjs",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "start": "func start",
    "lint": "eslint --fix \"**/*.js\" && prettier -w \"**/*.{js,css,md,json}\" --ignore-path .gitignore",
    "test": "jest --clearCache && jest",
    "test:watch": "jest --watchAll",
    "ci:lint": "eslint \"**/*.js\" && prettier -c \"**/*.{js,css,md,json}\" --ignore-path .gitignore",
    "ci:test": "jest --ci"
  },
  "dependencies": {
    "coingecko-api": "^1.0.10",
    "serverless-telegram": "github:miridius/serverless-telegram"
  },
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "eslint": "^7.17.0",
    "eslint-plugin-jest": "^24.1.3",
    "husky": "^3.1.0",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.3",
    "nock": "^13.0.5",
    "prettier": "^2.2.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix",
    "*.{js,css,md,json}": "prettier --write"
  }
}
